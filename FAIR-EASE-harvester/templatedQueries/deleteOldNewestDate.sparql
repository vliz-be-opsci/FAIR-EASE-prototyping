PREFIX dct: <http://purl.org/dc/terms/>
PREFIX xsd: <http://www.w3.org/2001/XMLSchema#>

DELETE {
  GRAPH <{{adminNode}}> {  
    <{{baseUrl}}> dct:modified ?newestDate .
  }
} WHERE {
  GRAPH <{{adminNode}}> {
    <{{baseUrl}}> dct:modified ?newestDate .
  }
}
